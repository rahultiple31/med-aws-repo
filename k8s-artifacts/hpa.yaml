apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cdk-web
  namespace: sbt-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cdk-web
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
